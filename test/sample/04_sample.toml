# simple

[[databases]]
d_name = "school"

# collection: student
[databases.collection]
c_name = "student"
count = 1000

# collection: student field:s_id
[[databases.collection.fields]]
constraints = [{type = "objectID"}]
f_name = "_id"

# collection: student field:s_id
[[databases.collection.fields]]
constraints = [{value = "M", weight = 1}, {value = "F", weight = 4}]
f_name = "sex"